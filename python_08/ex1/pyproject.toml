[tool.poetry]
name = "loading"
version = "0.1.0"
description = "The Matrix - Loading Programs"
authors = ["acombier"]

[tool.poetry.dependencies]
python = ">=3.11"
pandas = "3.0.2"
numpy = "2.3.3"
matplotlib = "3.10.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
